﻿@model Pet_Management_System.Models.CustomerOrder
@{
    ViewBag.Title = "AddressAndPayment";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<script src="@Url.Content("~/Scripts2/jquery.validate.min.js")"
        type="text/javascript"></script>
<script src="@Url.Content("~/Scripts2/jquery.validate.unobtrusive.min.js")"
        type="text/javascript"></script>
<section class="w3l-index-about py-5" id="about2">
    <div class="container py-md-5 py-2">
        @using (Html.BeginForm())
        {

            <h2>Address And Payment</h2>
            <fieldset>
                <legend>Shipping Information</legend>
                @Html.EditorForModel()
            </fieldset>
            @*<fieldset>
                    <legend>Payment</legend>
                    <p>
                        We're running a promotion: all item is free
                        with the promo code: "FREE"
                    </p>
                    <div class="editor-label">
                        @Html.Label("Promo Code")
                    </div>
                    <div class="editor-field">
                        @Html.TextBox("PromoCode")
                    </div>
                </fieldset>*@
            <div style="padding-top:3px">
                <input class="btn-outline-dark" type="submit" value="Submit Order" />
            </div>


        }
    </div>
   
</section>